 <%= form_with(model: [book, comment], url: book_book_comments_path(book)) do |f|%>
      <%= f.text_area :comment ,class: 'comment__textarea'%>
      <%= f.submit "送信", class: "btn btn-primary" %>
    <% end %>